<!-- Begin Page Content -->
<div class="container-fluid">

  <!-- Page Heading -->
  <h1 class="h3 mb-2 text-gray-800"x>Paytm Details</h1>
  <p class="mb-4">Configure your Paytm details</p>

  <!-- DataTales Example -->
  <div class="card shadow mb-4">
    <div class="card-header py-3">
      <h6 class="m-0 font-weight-bold text-primary">Paytm Settings</h6>
    </div>
    <div class="card-body">
        <form class="user" role="form" action="paytm" method="POST">
            <% if (messages.errors) { 0
            %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <% 
                    messages.errors.forEach(function(error) {
                %>
                <div> <%= error.msg ? error.msg : error  %> </div>
                <%
                    }); 
                %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
                </div>
            <% 
                }
            %>
            <div class="form-group" small>
                <label class="control-label" for="mId">MID</label>
                <input type="text" id="mId" class="form-control form-control-user" value="<%= mId %>" name="mId" placeholder="MID">
            </div>
            <div class="form-group" small>
                <label class="control-label" for="clientId">Merchant Key</label>
                <input type="text" id="merchantKey" class="form-control form-control-user" value="<%= merchantKey %>" name="merchantKey" placeholder="Merchant Key">
            </div>
            <div class="form-group" small>
                <label class="control-label" for="clientSecret">Website Name</label>
                <input type="text" id="websiteName" class="form-control form-control-user" value="<%= websiteName%>" name="websiteName" placeholder="Website Name">
            </div>
            <div class="form-group" small>
                <label class="control-label" for="clientSecret">Industry Type</label>
                <input type="text" id="industryType" class="form-control form-control-user" value="<%= industryType %>" name="industryType" placeholder="Industry Type">
            </div>
            <div class="form-group" small>
                <label class="control-label" for="clientSecret">Channel Id</label>
                <input type="text" id="channelId" class="form-control form-control-user" value="<%= channelId %>" name="channelId" placeholder="Channel Id">
            </div>
            <div class="form-group">
                <div class="form-group">
                    <div class="custom-control custom-checkbox small">
                    <input type="checkbox" value="1" name="isTest" class="custom-control-input" id="customCheck" <%= isTest ? 'checked' : '' %> />
                    <label class="custom-control-label" for="customCheck">Is Testing(Key for testing)</label>
                    </div>
                </div>
            </div>
            <button type="submit" class="btn btn-primary btn-user btn-sm">
                Save
            </button>
            <button onclick="location.href = 'home';" type="button" class="btn btn-info btn-user btn-sm" >
                Cancel
            </button>
        </form>
    </div>
  </div>

</div>
<!-- /.container-fluid -->